{
  // Solution-style: the app and its build config are separate projects with
  // different globals. Referenced projects are what `tsc --build` and the
  // linter's project service both follow.
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }]
}
